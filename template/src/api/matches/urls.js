import baseUrl from '../baseUrl'

export default {
    Activitytime:baseUrl + 'pro/init',
    wx_get_sdk_config:baseUrl + 'wx_get_sdk_config',
    checkMyPrize:baseUrl + 'prize/checkMyPrize',
    randomPrize:baseUrl + 'prize/randomPrize',
    MyPrize:baseUrl + 'prize/getMyPrize',
}
